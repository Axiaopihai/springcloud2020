server:
  port: 80

spring:
  application:
    name: cloud-consumer-openfeign

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka

ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000

logging:
  level:
    com.myself.feigninterface.FeignInte: debug

